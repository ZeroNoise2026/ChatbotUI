fastapi
uvicorn[standard]
supabase
python-dotenv
openai
requests
pydantic
pyjwt[crypto]>=2.8

# File parsing (Phase 2)
python-multipart>=0.0.9
openpyxl>=3.1
pypdf>=4.2
python-docx>=1.1
chardet>=5.2

# Shared skill framework — lives in its own repo (ZeroNoise2026/Skills).
# Installed from git rather than COPY'd, because the Docker build context is
# this backend/ directory and can never reach a sibling repo. Without this the
# `import skills` in main.py fails inside the image and skill routing is
# silently dead in Cloud Run.
quantagent-skills @ git+https://github.com/ZeroNoise2026/Skills.git@main
